#include <stdio.h>
#include "ring_buffer.h"

int main (void){

/*struct ring_buffer rb;
rb_init(&rb);

for (int i = 0; i<257; i++){
    rb_push(&rb, i);
}

printf("Newest: %d\n", rb_get(&rb, 0)); 
printf("Previous: %d\n", rb_get(&rb, 1)); 
printf("Oldest: %d\n", rb_get(&rb, 255)); 
*/

return 0;

}